var emoText = input text of user
var emotion = function () {
    if ()
}
var textColor = color associated with emotion
var textFont = font pool assosciated with emotion

funciton textConverter () {
    if
}